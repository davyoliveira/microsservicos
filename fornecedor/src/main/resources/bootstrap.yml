# esse yml é executado antes do application.yml, dessa forma, o que estiver aqui é carregado antes.

spring:
  application:
    name: 'fornecedor'
    
  profiles:
    active: default
    
  cloud:
    config:
      uri : http://localhost:8888
